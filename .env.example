# =========================================================================
# KH Cloud Environment Configuration Example
# Copy this file to '.env' and populate with your production settings.
# Do NOT commit your actual '.env' file to public source control.
# =========================================================================

# --- DOMAIN SETTINGS ---
# The base domain where your cloud is hosted.
BASE_DOMAIN=yourdomain.com

# --- GOOGLE OAUTH CONFIGURATION ---
# Used for user authentication (signing in).
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- GITHUB OAUTH CONFIGURATION (LEGACY / OPTIONAL AUTH) ---
# Used if you want to support login via GitHub.
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# --- GITHUB APP INTEGRATION ---
# Essential for Vercel/Railway-style selective repository import and auto-deployments.
# Create the app at: https://github.com/settings/apps/new
#
# Config settings for the App:
# - Homepage URL: https://cloud.yourdomain.com
# - Callback URL: https://cloud.yourdomain.com  (or your API callback if used)
# - Setup URL (Post installation): https://cloud.yourdomain.com
# - Webhook URL: https://api.yourdomain.com/api/github/webhook
# - Check: "Redirect on update"
# - Where can this GitHub App be installed?: "Any account" (required for org installs)
#
# Permissions:
# - Repository:
#   * Contents: Read-only
#   * Metadata: Read-only
#   * Webhooks: Read & write
# - Events Subscription:
#   * Check "Push" event.
#
# After creating the app, install it on each GitHub Organization whose private
# repos you want to deploy. Personal OAuth tokens cannot clone org private repos.

# GitHub App ID (numeric identifier)
GITHUB_APP_ID=1234567

# GitHub App Slug (url suffix, e.g. 'my-cloud-app' in github.com/apps/my-cloud-app)
GITHUB_APP_SLUG=your-github-app-slug

# GitHub App Client ID (for identification)
GITHUB_APP_CLIENT_ID=Iv23liDQkhKe8l...

# GitHub App Client Secret
GITHUB_APP_CLIENT_SECRET=your_github_app_client_secret

# GitHub App Private Key (PEM format, multi-line enclosed in quotes)
# Read the downloaded .pem key file, copy everything, and replace real newlines with '\n'
# Example format: "-----BEGIN RSA PRIVATE KEY-----\nMIIE...base64...\n-----END RSA PRIVATE KEY-----"
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYOUR_RSA_KEY_HERE\n-----END RSA PRIVATE KEY-----"

# GitHub App Webhook Secret (HMAC verification)
# Must match the string set in the "Webhook Secret" box of the GitHub App settings page.
GITHUB_APP_WEBHOOK_SECRET=your_webhook_hmac_secret_string

# --- OPTIONAL NEXT.js ENVS (Defaults to fallbacks, but can override) ---
NEXT_PUBLIC_GITHUB_CLIENT_ID=your-github-client-id
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ==========================================================================
# 🛡️ DDoS PROTECTION CONFIGURATION
# ==========================================================================

# Redis connection used by the DDoS guard (sliding-window rate limiter).
# In production Docker Compose this is already set to redis://redis:6379.
REDIS_URL=redis://redis:6379

# ---- Rate limits (requests per minute per IP) ----
# Auth routes  (register, login) — strictest limit
DDOS_AUTH_LIMIT=10
# API routes (/api/*)
DDOS_API_LIMIT=60
# All other routes (frontend, CDN assets, etc.)
DDOS_GLOBAL_LIMIT=200

# ---- Auto-ban settings ----
# How many times an IP must exceed the limit before it is automatically banned
DDOS_BAN_THRESHOLD=5
# How long the ban lasts in seconds (3600 = 1 hour)
DDOS_BAN_TTL_SECONDS=3600

# ---- Discord alerts ----
# When an IP is auto-banned, a notification is posted to this webhook URL.
# Leave empty to disable ban alerts.
DDOS_ALERT_DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ---- Admin API ----
# Secret key required for DDoS admin endpoints (GET /api/admin/ddos/stats, etc.)
# Send as the 'x-admin-key' header. Generate a strong random string.
ADMIN_API_KEY=your-strong-random-admin-key-here
